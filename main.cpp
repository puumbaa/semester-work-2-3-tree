#include <iostream>
#include "data_structure.hpp"
#include "Node.hpp"

using namespace itis;

int main(int argc, char *argv[]) {
    new DataStructure(new Node(2,))
  for (int index = 0; index < argc; index++) {
    std::cout << index << ": " << argv[index] << std::endl;
  }
  return 0;
}